#ifndef shared_bridge_h
#define shared_bridge_h

// Swift 代码与 Kotlin/Native XCFramework 之间不需要显式 Objective-C 桥接，
// 因为 Kotlin/Native 直接生成 Swift 可见的 API（Kotlin -> Swift mapping）。
// 此文件占位，保留未来扩展空间。

#endif
